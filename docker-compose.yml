version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: minesafe_backend
    env_file:
      - .env  # Load from .env file (create from .env.example)
    ports:
      - "8080:8080"
    restart: unless-stopped
    network_mode: host
