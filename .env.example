# Database Configuration
# For production (Render/Heroku), use DATABASE_URL
# For local development, use individual DB variables
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# Or use individual variables (local development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=endkot
DB_PASSWORD=endkot
DB_NAME=toursafe
DB_SSLMODE=disable

# Server Configuration
PORT=8080

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-min-32-chars

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-frontend-domain.vercel.app

# LocationIQ API Key for reverse geocoding
LOCATIONIQ_API_KEY=your-locationiq-api-key-here
